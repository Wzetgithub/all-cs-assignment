public class circle {
    public static void main(String[] args) {
        StdOut.print("Enter a number: ");
        int numCircle = StdIn.readInt();
        for(int i = numCircle ; i>0; i--) {
        StdDraw.setPenColor(i, (2*i), (3*i));
            
        StdDraw.filledCircle(.5, .5, (.45-);

        }
    }
}
